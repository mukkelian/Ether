# Wannier program's Makefile has been used in building this Makefile
# Ref:: (http://www.wannier.org/download/)

#--- gnu
f90comp = mpif90
switch = -O3 -fopenmp

#--- for gnu debug
# switch = -O0 -fopenmp -g -fcheck=all

#--- intel
# f90comp = mpiifx
# switch = -O3 -qopenmp

#--- for intel debug
# switch = -O0 -qopenmp -check all -warn unused -debug extended \
	-check bounds -check uninit -check arg_temp_created
